#include<stdio.h>

int  main (void) {


   int c;


fprintf(stdout,"%-44s", "Greg");

/*
   while (1) {
      c = fgetc(stdin);
      if ((c == EOF) || (c == '\n'))
         break;
      printf(" %d ",c);
   }
  */
   return 0;
}
